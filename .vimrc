"All the configs are to be shoved into ~/.vim/cfgs/ with a .vim extension
so ~/.vim/cfgs/vundle.vim
so ~/.vim/cfgs/markdown.vim
so ~/.vim/cfgs/basics.vim
so ~/.vim/cfgs/tabedits.vim
so ~/.vim/cfgs/tags.vim
